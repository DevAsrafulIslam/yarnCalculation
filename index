<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Yarn Calculator</title>

    <style>
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }

      body {
        font-family:
          Inter,
          -apple-system,
          BlinkMacSystemFont,
          "Segoe UI",
          Arial,
          sans-serif;

        background: #f5f7fa;
        color: #172033;
        min-height: 100vh;
        padding: 24px 16px;
      }

      /* =========================
       MAIN CARD
    ========================= */

      .card {
        width: 100%;
        max-width: 1150px;
        margin: auto;
        background: #ffffff;
        border: 1px solid #e8ebf0;
        border-radius: 18px;
        box-shadow: 0 8px 35px rgba(15, 23, 42, 0.07);
        overflow: hidden;
      }

      /* =========================
       HEADER
    ========================= */

      .header {
        padding: 30px 32px 24px;
        border-bottom: 1px solid #edf0f4;
        background: #ffffff;
      }

      .header h1 {
        font-size: 28px;
        font-weight: 700;
        letter-spacing: -0.5px;
        color: #111827;
      }

      .header p {
        margin-top: 7px;
        color: #6b7280;
        font-size: 14px;
      }

      /* =========================
       CONTENT
    ========================= */

      .content {
        padding: 28px 32px 32px;
      }

      .section {
        margin-bottom: 30px;
      }

      .section:last-child {
        margin-bottom: 0;
      }

      .section-title {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 17px;
      }

      .section-number {
        width: 28px;
        height: 28px;
        border-radius: 8px;
        background: #111827;
        color: #ffffff;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 13px;
        font-weight: 700;
        flex-shrink: 0;
      }

      .section-title h2 {
        font-size: 17px;
        font-weight: 700;
        color: #1f2937;
      }

      /* =========================
       GRID
    ========================= */

      .grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 16px;
      }

      .field {
        min-width: 0;
      }

      label {
        display: block;
        font-size: 13px;
        font-weight: 600;
        color: #374151;
        margin-bottom: 7px;
      }

      input {
        width: 100%;
        height: 44px;
        padding: 0 13px;

        border: 1px solid #d9dee7;
        border-radius: 9px;

        background: #ffffff;
        color: #111827;

        font-size: 14px;
        outline: none;

        transition:
          border-color 0.2s ease,
          box-shadow 0.2s ease,
          background 0.2s ease;
      }

      input::placeholder {
        color: #9ca3af;
      }

      input:hover {
        border-color: #bfc6d1;
      }

      input:focus {
        border-color: #111827;
        box-shadow: 0 0 0 3px rgba(17, 24, 39, 0.08);
      }

      /* =========================
       NOTE
    ========================= */

      .note {
        margin-top: 10px;
        padding: 10px 12px;

        background: #f8fafc;
        border: 1px solid #edf0f4;
        border-radius: 8px;

        color: #6b7280;
        font-size: 12px;
        line-height: 1.6;
      }

      /* =========================
       RADIO
    ========================= */

      .radio-wrapper {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
      }

      .radio-option {
        position: relative;
      }

      .radio-option input {
        position: absolute;
        opacity: 0;
        pointer-events: none;
      }

      .radio-option label {
        display: flex;
        align-items: center;
        gap: 8px;

        height: 42px;
        padding: 0 15px;

        border: 1px solid #d9dee7;
        border-radius: 9px;

        background: #ffffff;
        color: #4b5563;

        font-size: 13px;
        font-weight: 600;

        cursor: pointer;
        margin: 0;

        transition: all 0.2s ease;
      }

      .radio-option label::before {
        content: "";
        width: 15px;
        height: 15px;
        border: 1.5px solid #9ca3af;
        border-radius: 50%;
        transition: all 0.2s ease;
      }

      .radio-option input:checked + label {
        border-color: #111827;
        background: #111827;
        color: #ffffff;
      }

      .radio-option input:checked + label::before {
        border: 4px solid #ffffff;
        background: #111827;
      }

      /* =========================
       SUB SECTION
    ========================= */

      .sub-section {
        margin-top: 16px;
        padding: 18px;

        border: 1px solid #edf0f4;
        border-radius: 12px;

        background: #fafbfc;
      }

      .sub-section h3 {
        font-size: 14px;
        margin-bottom: 14px;
        color: #374151;
      }

      /* =========================
       DISTRIBUTION
    ========================= */

      .distribution {
        margin-top: 16px;
      }

      .distribution-title {
        font-size: 13px;
        font-weight: 600;
        color: #374151;
        margin-bottom: 8px;
      }

      /* =========================
       CALCULATE BUTTON
    ========================= */

      .calculate-btn {
        width: 100%;
        height: 48px;

        border: none;
        border-radius: 10px;

        background: #111827;
        color: #ffffff;

        font-size: 14px;
        font-weight: 700;
        letter-spacing: 0.2px;

        cursor: pointer;

        transition:
          transform 0.15s ease,
          background 0.2s ease,
          box-shadow 0.2s ease;
      }

      .calculate-btn:hover {
        background: #1f2937;
        box-shadow: 0 7px 18px rgba(17, 24, 39, 0.18);
      }

      .calculate-btn:active {
        transform: scale(0.99);
      }

      /* =========================
       ERROR
    ========================= */

      .error {
        display: none;
        margin-top: 14px;

        padding: 12px 14px;

        border-radius: 9px;
        border: 1px solid #fecaca;

        background: #fef2f2;
        color: #b42318;

        font-size: 13px;
        line-height: 1.5;
        font-weight: 600;
      }

      /* =========================
       RESULT
    ========================= */

      #result {
        margin-top: 30px;
        padding-top: 28px;
        border-top: 1px solid #edf0f4;
      }

      .result-title {
        font-size: 20px;
        font-weight: 700;
        margin-bottom: 16px;
        color: #111827;
      }

      /* =========================
       STATUS
    ========================= */

      .ok,
      .bad {
        padding: 13px 15px;
        border-radius: 9px;

        font-size: 13px;
        font-weight: 600;

        margin-bottom: 16px;
      }

      .ok {
        background: #ecfdf3;
        border: 1px solid #bbf7d0;
        color: #087443;
      }

      .bad {
        background: #fef2f2;
        border: 1px solid #fecaca;
        color: #b42318;
      }

      /* =========================
       TABLE
    ========================= */

      .table-wrapper {
        width: 100%;
        overflow-x: auto;
        border: 1px solid #dfe4ea;
        border-radius: 8px;
      }

      table {
        width: 100%;
        min-width: 760px;
        table-layout: fixed;
        border-collapse: collapse;
        font-size: 11px;
      }

      /* TABLE HEADER */

      th {
        padding: 9px 7px;
        background: #111827;
        color: #ffffff;
        font-size: 10px;
        font-weight: 600;
        text-align: center;
        white-space: nowrap;
        border: 1px solid #111827;
      }

      /* TABLE DATA */

      td {
        padding: 9px 7px;
        text-align: center;
        border: 1px solid #e5e7eb;
        color: #374151;
        background: #ffffff;
        white-space: nowrap;
      }

      /* COLUMN WIDTH */

      th:nth-child(1),
      td:nth-child(1) {
        width: 13%;
      }

      th:nth-child(2),
      td:nth-child(2) {
        width: 10%;
      }

      th:nth-child(3),
      td:nth-child(3) {
        width: 10%;
      }

      th:nth-child(4),
      td:nth-child(4) {
        width: 14%;
      }

      th:nth-child(5),
      td:nth-child(5) {
        width: 18%;
      }

      th:nth-child(6),
      td:nth-child(6) {
        width: 13%;
      }

      th:nth-child(7),
      td:nth-child(7) {
        width: 12%;
      }

      /* HOVER */

      tbody tr:hover td {
        background: #f8fafc;
      }

      /* =========================
       FOOTER
    ========================= */

      .footer {
        text-align: center;
        padding: 18px 20px;
        border-top: 1px solid #edf0f4;

        color: #9ca3af;
        font-size: 11px;
      }

      /* =========================
       DESKTOP
    ========================= */

      @media (min-width: 900px) {
        .content {
          padding: 32px 40px 40px;
        }

        .header {
          padding: 34px 40px 27px;
        }

        .header h1 {
          font-size: 30px;
        }

        .calculate-btn {
          width: 220px;
          display: block;
          margin-left: auto;
        }
      }

      /* =========================
       TABLET
    ========================= */

      @media (max-width: 750px) {
        body {
          padding: 12px;
        }

        .card {
          border-radius: 14px;
        }

        .header {
          padding: 23px 20px;
        }

        .content {
          padding: 23px 20px 25px;
        }

        .header h1 {
          font-size: 23px;
        }

        .grid {
          grid-template-columns: 1fr;
          gap: 13px;
        }

        .section {
          margin-bottom: 26px;
        }

        .sub-section {
          padding: 15px;
        }

        .calculate-btn {
          height: 46px;
        }
      }

      /* =========================
       MOBILE
    ========================= */

      @media (max-width: 480px) {
        body {
          padding: 8px;
        }

        .card {
          border-radius: 12px;
        }

        .header {
          padding: 20px 16px;
        }

        .content {
          padding: 20px 16px 22px;
        }

        .header h1 {
          font-size: 21px;
        }

        .header p {
          font-size: 12px;
        }

        .section-title {
          gap: 8px;
        }

        .section-number {
          width: 25px;
          height: 25px;
          font-size: 11px;
        }

        .section-title h2 {
          font-size: 15px;
        }

        input {
          height: 43px;
          font-size: 14px;
        }

        .radio-wrapper {
          display: grid;
          grid-template-columns: 1fr 1fr;
        }

        .radio-option label {
          justify-content: center;
          padding: 0 8px;
          font-size: 12px;
        }

        .note {
          font-size: 11px;
        }

        .result-title {
          font-size: 18px;
        }

        .footer {
          padding: 15px;
        }
      }
    </style>
  </head>

  <body>
    <div class="card">
      <!-- HEADER -->
      <div class="header">
        <h1>Yarn Calculator</h1>
        <p>Calculate TE, cone requirement and yarn consumption easily.</p>
      </div>

      <div class="content">
        <!-- =========================
           TE CALCULATION
      ========================== -->

        <div class="section">
          <div class="section-title">
            <div class="section-number">1</div>
            <h2>TE Calculation</h2>
          </div>

          <div class="grid">
            <div class="field">
              <label for="te"
                >TE <span style="color: #9ca3af">(Optional)</span></label
              >

              <input id="te" type="number" step="any" placeholder="e.g. 5536" />
            </div>

            <div class="field">
              <label for="reed">Reed Space</label>

              <input
                id="reed"
                type="number"
                step="any"
                placeholder="Required if TE is empty"
              />
            </div>

            <div class="field">
              <label for="epi">EPI</label>

              <input
                id="epi"
                type="number"
                step="any"
                placeholder="Required if TE is empty"
              />
            </div>
          </div>

          <div class="note">
            TE থাকলে EPI ও Reed Space খালি রাখা যাবে। TE খালি থাকলে EPI + Reed
            Space দুটোই দিতে হবে।
          </div>
        </div>

        <!-- ========================= TOTAL BALL & CONE ========================== -->

        <div class="section">
          <div class="section-title">
            <div class="section-number">2</div>
            <h2>Total Ball</h2>
          </div>

          <div class="grid">
            <div class="field">
              <label for="totalBall">Total Ball</label>

              <input id="totalBall" type="number" step="any" value="12" />
            </div>

            <div class="field" style="display: none !important">
              <label for="minCone">Minimum Cone (Dyne)</label>

              <input id="minCone" type="number" value="340" />
            </div>

            <div class="field" style="display: none !important">
              <label for="maxCone">Maximum Cone (Dyne)</label>

              <input id="maxCone" type="number" value="480" />
            </div>
          </div>

          <div class="note">
            Number of Cone = TE ÷ Total Ball. Decimal 0.5 or more হলে +1 হবে।
          </div>
        </div>

        <!-- =========================
           YARN TYPE
      ========================== -->

        <div class="section">
          <div class="section-title">
            <div class="section-number">3</div>
            <h2>Yarn Type</h2>
          </div>

          <div class="radio-wrapper">
            <div class="radio-option">
              <input
                type="radio"
                name="mode"
                id="singleMode"
                value="single"
                checked
                onchange="modeChange()"
              />

              <label for="singleMode"> Single Yarn </label>
            </div>

            <div class="radio-option">
              <input
                type="radio"
                name="mode"
                id="multipleMode"
                value="multiple"
                onchange="modeChange()"
              />

              <label for="multipleMode"> Multiple Yarn </label>
            </div>
          </div>

          <!-- SINGLE YARN -->

          <div class="sub-section" id="singleInput">
            <h3>Single Yarn</h3>

            <div class="grid">
              <div class="field">
                <label for="singleName"> Yarn Name </label>

                <input id="singleName" value="10RS" placeholder="10RS" />
              </div>

              <div class="field">
                <label for="singleCount"> Yarn Count </label>

                <input
                  id="singleCount"
                  type="number"
                  step="any"
                  value="10"
                  placeholder="10"
                />
              </div>
            </div>
          </div>

          <!-- MULTIPLE YARN -->

          <div class="sub-section" id="multipleInput" style="display: none">
            <h3>Multiple Yarn</h3>

            <div class="grid">
              <div class="field">
                <label for="names">
                  Yarn Name
                  <span style="color: #9ca3af"> (comma separated) </span>
                </label>

                <input
                  id="names"
                  value="10RS,10KW"
                  placeholder="10RS,10KW,12RS"
                />
              </div>

              <div class="field">
                <label for="counts">
                  Yarn Count
                  <span style="color: #9ca3af"> (comma separated) </span>
                </label>

                <input id="counts" value="10,10" placeholder="10,10,12" />
              </div>
            </div>

            <div class="distribution">
              <div class="distribution-title">Distribution</div>

              <div class="radio-wrapper">
                <div class="radio-option">
                  <input
                    type="radio"
                    name="dist"
                    id="ratioDist"
                    value="ratio"
                    checked
                    onchange="distChange()"
                  />

                  <label for="ratioDist"> Ratio </label>
                </div>

                <div class="radio-option">
                  <input
                    type="radio"
                    name="dist"
                    id="percentDist"
                    value="percent"
                    onchange="distChange()"
                  />

                  <label for="percentDist"> % </label>
                </div>
              </div>

              <div id="ratioBox" style="margin-top: 10px">
                <input
                  id="ratio"
                  value="1:2"
                  placeholder="1:2 / 1:1:1 / 2:4:6"
                />
              </div>

              <div id="percentBox" style="display: none; margin-top: 10px">
                <input
                  id="percent"
                  value="33,67"
                  placeholder="33,67 / 33,33,34"
                />
              </div>
            </div>
          </div>
        </div>

        <!-- =========================
           YARN REQUIREMENT
      ========================== -->

        <div class="section">
          <div class="section-title">
            <div class="section-number">4</div>
            <h2>Yarn Requirement</h2>
          </div>

          <div class="grid">
            <div class="field">
              <label for="fabric"> Fabric Length (Meter) </label>

              <input id="fabric" type="number" step="any" value="1000" />
            </div>

            <div class="field">
              <label for="wastage"> Wastage (Meter) </label>

              <input id="wastage" type="number" step="any" value="500" />
            </div>

            <div class="field" style="display: none !important">
              <label for="constant"> Constant </label>

              <input id="constant" type="number" step="any" value="0.00059" />
            </div>
          </div>
        </div>

        <!-- =========================
           CALCULATE
      ========================== -->

        <button class="calculate-btn" onclick="calculate()">CALCULATE</button>

        <!-- ERROR -->

        <div id="error" class="error"></div>

        <!-- =========================
           RESULT
      ========================== -->

        <div id="result" style="display: none">
          <div class="result-title">Final Result</div>

          <div id="coneStatus"></div>

          <div class="table-wrapper">
            <table>
              <thead id="thead"></thead>

              <tbody id="tbody"></tbody>
            </table>
          </div>
        </div>
      </div>

      <div class="footer">© 2026 Asraful_Islam All Rights Reserved.</div>
    </div>

    <script>
      /* =========================
       ROUND HALF UP
    ========================== */

      function roundHalfUp(x) {
        return Math.floor(x + 0.5);
      }

      /* =========================
       SPLIT VALUES
    ========================== */

      function vals(s) {
        return s
          .split(",")
          .map((x) => x.trim())
          .filter((x) => x !== "");
      }

      /* =========================
       MODE CHANGE
    ========================== */

      function modeChange() {
        const m = document.querySelector('input[name="mode"]:checked').value;

        document.getElementById("singleInput").style.display =
          m === "single" ? "block" : "none";

        document.getElementById("multipleInput").style.display =
          m === "multiple" ? "block" : "none";
      }

      /* =========================
       DISTRIBUTION CHANGE
    ========================== */

      function distChange() {
        const d = document.querySelector('input[name="dist"]:checked').value;

        document.getElementById("ratioBox").style.display =
          d === "ratio" ? "block" : "none";

        document.getElementById("percentBox").style.display =
          d === "percent" ? "block" : "none";
      }

      /* =========================
       CALCULATE
    ========================== */

      function calculate() {
        const error = document.getElementById("error");

        error.textContent = "";
        error.style.display = "none";

        const teInput = document.getElementById("te").value.trim();

        const reedInput = document.getElementById("reed").value.trim();

        const epiInput = document.getElementById("epi").value.trim();

        const reed = reedInput === "" ? null : Number(reedInput);

        const epi = epiInput === "" ? null : Number(epiInput);

        const total = +document.getElementById("totalBall").value;

        const min = +document.getElementById("minCone").value;

        const max = +document.getElementById("maxCone").value;

        const fabric = +document.getElementById("fabric").value;

        const wastage = +document.getElementById("wastage").value;

        const constant = +document.getElementById("constant").value;

        const type = document.querySelector('input[name="mode"]:checked').value;

        /* =========================
         BASIC VALIDATION
      ========================== */

        if (!total || !fabric || wastage < 0 || constant <= 0) {
          error.textContent =
            "Total Ball, Fabric Length, Wastage এবং Constant সঠিকভাবে দিন।";

          error.style.display = "block";

          return;
        }

        /* =========================
         TE
      ========================== */

        let TE;

        if (teInput !== "") {
          TE = Number(teInput);

          if (!Number.isFinite(TE) || TE <= 0) {
            error.textContent = "TE সঠিকভাবে দিন।";

            error.style.display = "block";

            return;
          }
        } else {
          if (reedInput === "" || epiInput === "") {
            error.textContent =
              "TE না থাকলে EPI এবং Reed Space দুটোই দিতে হবে।";

            error.style.display = "block";

            return;
          }

          if (
            !Number.isFinite(reed) ||
            reed <= 0 ||
            !Number.isFinite(epi) ||
            epi <= 0
          ) {
            error.textContent = "Reed Space এবং EPI সঠিকভাবে দিন।";

            error.style.display = "block";

            return;
          }

          TE = reed * epi;
        }

        /* =========================
         CONE
      ========================== */

        const cone = roundHalfUp(TE / total);

        let names = [];
        let counts = [];
        let pct = [];
        let distribution = [];

        /* =========================
         SINGLE YARN
      ========================== */

        if (type === "single") {
          const name = document.getElementById("singleName").value.trim();

          const count = +document.getElementById("singleCount").value;

          if (!name || !count || count <= 0) {
            error.textContent = "Single Yarn-এর Name এবং Count দিন।";

            error.style.display = "block";

            return;
          }

          names = [name];
          counts = [count];
          pct = [100];
        } else {
          /* =========================
         MULTIPLE YARN
      ========================== */
          names = vals(document.getElementById("names").value);

          counts = vals(document.getElementById("counts").value).map(Number);

          if (!names.length || names.length !== counts.length) {
            error.textContent = `Yarn Name ${names.length}টি এবং Count ${counts.length}টি। দুইটার সংখ্যা একই হতে হবে।`;

            error.style.display = "block";

            return;
          }

          if (counts.some((x) => !Number.isFinite(x) || x <= 0)) {
            error.textContent = "Count-এ শুধু valid number দিন।";

            error.style.display = "block";

            return;
          }

          const dist = document.querySelector(
            'input[name="dist"]:checked',
          ).value;

          /* RATIO */

          if (dist === "ratio") {
            distribution = document
              .getElementById("ratio")
              .value.trim()
              .split(":")
              .map(Number);

            if (
              distribution.length !== names.length ||
              distribution.some((x) => !Number.isFinite(x) || x <= 0)
            ) {
              error.textContent =
                "Ratio-এর সংখ্যা Yarn-এর সংখ্যার সমান হতে হবে।";

              error.style.display = "block";

              return;
            }

            const sum = distribution.reduce((a, b) => a + b, 0);

            pct = distribution.map((x) => (x / sum) * 100);
          } else {
            /* PERCENT */
            pct = vals(document.getElementById("percent").value).map(Number);

            if (
              pct.length !== names.length ||
              pct.some((x) => !Number.isFinite(x) || x < 0)
            ) {
              error.textContent =
                "Percentage-এর সংখ্যা Yarn-এর সংখ্যার সমান হতে হবে।";

              error.style.display = "block";

              return;
            }

            const sum = pct.reduce((a, b) => a + b, 0);

            if (Math.abs(sum - 100) > 0.01) {
              error.textContent = `Percentage total 100% হতে হবে। এখন ${sum.toFixed(2)}%`;

              error.style.display = "block";

              return;
            }

            distribution = pct;
          }
        }

        /* =========================
         CONE STATUS
      ========================== */

        const st = document.getElementById("coneStatus");

        if (cone >= min && cone <= max) {
          st.className = "ok";

          st.textContent = `✓ Cone ${cone} — ${min}–${max} limit-এর মধ্যে।`;
        } else {
          st.className = "bad";

          st.textContent = `✗ Cone ${cone} — ${min}–${max} limit-এর বাইরে।`;
        }

        /* =========================
         TABLE
      ========================== */

        const thead = document.getElementById("thead");

        const tbody = document.getElementById("tbody");

        tbody.innerHTML = "";

        /* =========================
         SINGLE TABLE
      ========================== */

        if (type === "single") {
          thead.innerHTML = `
          <tr>
            <th>Yarn</th>
            <th>Count</th>
            <th>Ball</th>
            <th>Number of Cone</th>
            <th>Yarn Length (m)</th>
            <th>Yarn KG</th>
            <th>Gram</th>
          </tr>
        `;

          const ball = roundHalfUp(total);

          const yarnLength = fabric * ball + wastage;

          const kg = (yarnLength * cone * constant) / counts[0];

          const gram = kg / cone;

          tbody.innerHTML = `
          <tr>

            <td>
              <b>${names[0]}</b>
            </td>

            <td>
              ${counts[0]}
            </td>

            <td>
              <b>${ball}</b>
            </td>

            <td>
              ${cone}
            </td>

            <td>
              ${yarnLength.toFixed(2)}
            </td>

            <td>
              ${kg.toFixed(3)}
            </td>

            <td>
              <b>${gram.toFixed(2)} g</b>
            </td>

          </tr>
        `;
        } else {
          /* =========================
         MULTIPLE TABLE
      ========================== */
          thead.innerHTML = `
          <tr>
            <th>Yarn</th>
            <th>Count</th>
            <th>Value %</th>
            <th>Ball</th>
            <th>Cone/Pcs</th>
            <th>Yarn Length (m)</th>
            <th>Yarn KG</th>
            <th>Gram</th>
          </tr>
        `;

          names.forEach((name, i) => {
            const ball = roundHalfUp((total * pct[i]) / 100);

            const yarnLength = fabric * ball + wastage;

            const kg = (yarnLength * cone * constant) / counts[i];

            const gram = kg / cone;

            tbody.innerHTML += `
              <tr>

                <td>
                  <b>${name}</b>
                </td>

                <td>
                  ${counts[i]}
                </td>

                <td>
                  ${pct[i].toFixed(2)}%
                </td>

                <td>
                  <b>${ball}</b>
                </td>

                <td>
                  ${cone}
                </td>

                <td>
                  ${yarnLength.toFixed(2)}
                </td>

                <td>
                  ${kg.toFixed(3)}
                </td>

                <td>
                  <b>${gram.toFixed(2)} g</b>
                </td>

              </tr>
            `;
          });
        }

        /* =========================
         SHOW RESULT
      ========================== */

        document.getElementById("result").style.display = "block";

        /* Scroll to result on mobile */

        setTimeout(() => {
          document.getElementById("result").scrollIntoView({
            behavior: "smooth",
            block: "start",
          });
        }, 100);
      }

      /* =========================
       INITIAL STATE
    ========================== */

      modeChange();
      distChange();
    </script>
  </body>
</html>
